beforeEach(async () => {
  // await db.seed.run();
});

afterAll(async () => {
  // await db.destroy();
});

describe('User Controller', () => {
  describe('GET /topActiveUsers', () => {
    it('x', () => {
      expect(true).to.be.true;
    });
  });
});
